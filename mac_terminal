GITHUB PUSH 
(In terminal)
	
	(git status)

	git add .
	git commit -m “message”
	git push

	git pull

	git checkout FILENAME

CREATE VIRTUAL ENVIRONMENTS
	
	python -m vent pyenv

LAUNCH JUPYTER NOTEBOOKS

	jupyter notebook


LAUNCH APPs

	source kollector/bin/activate
python3 src/app/app.py
